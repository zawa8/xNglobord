<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for voice input (speech recognition) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature
        android:name="android.software.input_methods"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_placeholder"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- Settings screen (font picker) for the IME, reachable via the
             gear icon in Settings > Languages & input > On-screen
             keyboard > xNglobord. -->
        <activity
            android:name=".SettingsActivity"
            android:label="@string/app_name"
            android:exported="true" />

        <!-- The IME itself. -->
        <service
            android:name=".XngloIME"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true"
            android:settingsActivity="com.xnglo.bord.SettingsActivity">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

</manifest>