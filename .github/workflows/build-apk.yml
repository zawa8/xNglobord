name: Build Android APK

on:
  push:
    branches:
      - zemini_mic
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Add your build steps here (e.g., set up JDK, build with Gradle, upload artifact)
